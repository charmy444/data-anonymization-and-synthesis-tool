# SDA (Synthetic Data Anonymizer)

Простой учебный проект для кейса: генерация синтетических CSV и анонимизация CSV без использования реальных персональных данных.

## Что это за проект

`SDA` = `Synthetic Data Anonymizer`  
Инструмент для **синтетических данных** и **анонимизации**.

Цель MVP:
1. Сгенерировать тестовые CSV (например, `users.csv`, `orders.csv`).
2. Анонимизировать загруженный CSV по правилам колонок:
   - masking (маскирование),
   - redaction (удаление/скрытие),
   - pseudonymization (стабильная подмена).

## Текущий статус

Сейчас в репозитории готов **каркас архитектуры** (папки и файлы созданы), но логика в `.py` файлах пока не реализована.

## Архитектура проекта (простыми словами)

Ниже перечислены **все папки и файлы**, которые сейчас есть в проекте.

### Корень проекта

#### `pyproject.toml`
- Что это: главный файл настроек Python-проекта.
- Для чего нужен: зависимости, имя проекта, версия, конфигурация запуска Web/API.
- Что добавить дальше:
  - dev-зависимости (`pytest`, `ruff`, `mypy`),
  - настройки линтера/форматтера,
  - дополнительные entrypoint-команды при необходимости.

#### `LICENSE`
- Что это: лицензия проекта (правила использования кода).
- Для чего нужен: юридическая прозрачность.
- Что добавить дальше: обычно менять не нужно (только если команда решит выбрать другую лицензию).

#### `.gitignore`
- Что это: список файлов, которые Git не должен сохранять.
- Для чего нужен: не коммитить временные/локальные файлы.
- Что добавить дальше:
  - `output/`, `data/`, `*.csv` (если это временные тестовые файлы),
  - служебные файлы IDE.

### Папка `src/`

`src` = исходный код приложения.

#### `src/sda/`

`sda` = `Synthetic Data Anonymizer` (основной пакет приложения).

##### `src/sda/__init__.py`
- Что это: инициализация Python-пакета `sda`.
- Для чего нужен: чтобы Python видел папку как пакет.
- Что добавить дальше:
  - `__version__`,
  - экспорт ключевых сущностей (по необходимости).

#### `src/sda/core/`

`core` = бизнес-логика (сердце проекта, не зависящее от интерфейса).

##### `src/sda/core/__init__.py`
- Что это: инициализация пакета core.
- Для чего нужен: технический файл структуры.
- Что добавить дальше: обычно пусто.

#### `src/sda/core/domain/`

`domain` = предметная область (модели и ошибки проекта).

##### `src/sda/core/domain/__init__.py`
- Что это: инициализация domain-пакета.
- Для чего нужен: технический файл структуры.
- Что добавить дальше: по желанию экспорт моделей/ошибок.

##### `src/sda/core/domain/errors.py`
- Что это: собственные ошибки проекта.
- Для чего нужен: понятные сообщения об ошибках.
- Что добавить дальше:
  - `InvalidCsvError`, `UnknownColumnError`, `InvalidRuleError`,
  - удобное преобразование в «человеческие» тексты для API-ответов.

#### `src/sda/core/generation/`

`generation` = генерация синтетических данных.

##### `src/sda/core/generation/__init__.py`
- Что это: инициализация пакета генерации.
- Для чего нужен: технический файл.
- Что добавить дальше: обычно пусто.

##### `src/sda/core/generation/generator.py`
- Что это: логика генерации CSV.
- Для чего нужен: создание строк для `users.csv`, `orders.csv`.
- Что добавить дальше:
  - генерацию через `Faker`,
  - шаблоны минимум для 2 таблиц,
  - связь `orders.user_id -> users.id`.

#### `src/sda/core/anonymization/`

`anonymization` = логика анонимизации.

##### `src/sda/core/anonymization/__init__.py`
- Что это: инициализация пакета анонимизации.
- Для чего нужен: технический файл.
- Что добавить дальше: обычно пусто.

##### `src/sda/core/anonymization/rules.py`
- Что это: правила анонимизации (какой метод к какой колонке).
- Для чего нужен: централизованная конфигурация поведения.
- Что добавить дальше:
  - enum/константы методов (`mask`, `redact`, `pseudo`),
  - парсинг правил из JSON-запросов API.

##### `src/sda/core/anonymization/anonymizer.py`
- Что это: движок анонимизации значений.
- Для чего нужен: применяет правила к данным.
- Что добавить дальше:
  - маскирование email/телефонов,
  - редактирование (пустое значение/`[REDACTED]`),
  - псевдонимизацию (стабильный hash/salt или словарь).

#### `src/sda/core/ml/`

`ml` = вспомогательные ML/эвристические компоненты (опционально для MVP).

##### `src/sda/core/ml/__init__.py`
- Что это: инициализация пакета `ml`.
- Для чего нужен: технический файл.
- Что добавить дальше: обычно пусто.

##### `src/sda/core/ml/pii_suggester.py`
- Что это: модуль поиска PII (персональных данных) в колонках.
- Для чего нужен: рекомендация правил анонимизации.
- Что добавить дальше:
  - словарь «подозрительных» названий (`email`, `phone`, `name`),
  - анализ примеров значений,
  - оценка уверенности.

##### `src/sda/core/ml/synth_stub.py`
- Что это: заглушка для возможной «умной» генерации данных.
- Для чего нужен: точка расширения на будущее.
- Что добавить дальше:
  - интерфейс для продвинутого генератора,
  - сравнение распределений синтетики и шаблона.

#### `src/sda/io/`

`io` = `Input/Output` (чтение/запись файлов).

##### `src/sda/io/__init__.py`
- Что это: инициализация пакета IO.
- Для чего нужен: технический файл.
- Что добавить дальше: обычно пусто.

##### `src/sda/io/csv_read.py`
- Что это: чтение CSV из bytes/stream.
- Для чего нужен: безопасный и единый способ загрузки данных в web-слое.
- Что добавить дальше:
  - автоопределение разделителя,
  - обработку пустых файлов,
  - понятные исключения.

##### `src/sda/io/csv_write.py`
- Что это: запись CSV в bytes/stream.
- Для чего нужен: единый способ формировать ответ `text/csv` в API.
- Что добавить дальше:
  - выбор разделителя,
  - контроль кодировки,
  - подготовку для `StreamingResponse`.

#### `src/sda/use_cases/`

`use_cases` = сквозные сценарии приложения (слой между интерфейсами и `core`).

##### `src/sda/use_cases/generate_csv.py`
- Что это: use-case генерации CSV.
- Для чего нужен: единая точка запуска генерации для Web/API.
- Что добавить дальше:
  - входные параметры (шаблон, rows, seed),
  - вызов `core/generation`,
  - возврат результата в bytes/rows формате.

##### `src/sda/use_cases/anonymize_csv.py`
- Что это: use-case анонимизации CSV.
- Для чего нужен: единый сценарий для preview и полного экспорта.
- Что добавить дальше:
  - чтение CSV из bytes,
  - применение правил через `core/anonymization`,
  - возврат preview и итогового CSV.

##### `src/sda/use_cases/suggest_pii.py`
- Что это: use-case подсказки PII-полей.
- Для чего нужен: общая логика рекомендации правил для интерфейсов.
- Что добавить дальше:
  - анализ заголовков и примеров значений,
  - формирование JSON-правил по эвристикам.

#### `src/sda/resources/`

`resources` = встроенные шаблоны и пресеты проекта.

##### `src/sda/resources/templates/`
- Что это: шаблоны таблиц для генератора.
- Для чего нужен: хранить кейсовые схемы (`users`, `orders`) отдельно от кода.

###### `src/sda/resources/templates/users.json`
- Что это: шаблон таблицы пользователей.
- Для чего нужен: быстрый демо-сценарий генерации `users.csv`.
- Что добавить дальше:
  - список колонок,
  - параметры генерации (при необходимости).

###### `src/sda/resources/templates/orders.json`
- Что это: шаблон таблицы заказов.
- Для чего нужен: быстрый демо-сценарий генерации `orders.csv`.
- Что добавить дальше:
  - список колонок,
  - связь `user_id` с пользователями (если нужно в логике).

##### `src/sda/resources/presets/`
- Что это: готовые пресеты правил анонимизации.
- Для чего нужен: использовать типовые правила без ручной настройки.

###### `src/sda/resources/presets/default_anonymize.json`
- Что это: базовый пресет анонимизации.
- Для чего нужен: стартовый набор правил для MVP.
- Что добавить дальше:
  - методы для `email`, `phone`, `name`,
  - дополнительные кейсы по необходимости.

#### `src/sda/web/`

`web` = web/API-оболочка на FastAPI (валидация входа -> вызов use-case -> ответ).

##### `src/sda/web/__init__.py`
- Что это: инициализация web-пакета.
- Для чего нужен: технический файл структуры.
- Что добавить дальше: обычно пусто.

##### `src/sda/web/app.py`
- Что это: создание FastAPI-приложения.
- Для чего нужен: точка входа для `uvicorn`.
- Что добавить дальше:
  - создание `FastAPI()`,
  - подключение роутеров,
  - глобальные обработчики ошибок.

##### `src/sda/web/deps.py`
- Что это: зависимости web-слоя (settings, сервисы).
- Для чего нужен: централизовать DI и конфигурацию API.
- Что добавить дальше:
  - функции `get_settings`,
  - провайдеры зависимостей для роутеров.

##### `src/sda/web/routers/`

`routers` = API endpoints, сгруппированные по сценариям.

###### `src/sda/web/routers/health.py`
- Что это: endpoint проверки состояния.
- Для чего нужен: health-check (`/health`).
- Что добавить дальше:
  - простой ответ `status: ok`.

###### `src/sda/web/routers/generate.py`
- Что это: endpoints генерации.
- Для чего нужен: `GET /templates`, `POST /generate`.
- Что добавить дальше:
  - валидацию входа,
  - вызов use-case генерации,
  - отдачу CSV через stream/bytes.

###### `src/sda/web/routers/anonymize.py`
- Что это: endpoints анонимизации.
- Для чего нужен: `POST /anonymize/preview`, `POST /anonymize`.
- Что добавить дальше:
  - приём `UploadFile` + rules,
  - preview первых N строк,
  - возврат анонимизированного CSV.

###### `src/sda/web/routers/suggest.py`
- Что это: endpoint подсказки PII.
- Для чего нужен: рекомендации правил по загруженному CSV.
- Что добавить дальше:
  - вызов use-case suggest,
  - возврат JSON-правил.

##### `src/sda/web/schemas/`

`schemas` = Pydantic-модели запросов и ответов API.

###### `src/sda/web/schemas/generate.py`
- Что это: схемы для генерации.
- Для чего нужен: валидировать тело `POST /generate`.
- Что добавить дальше:
  - модель запроса `{template, rows, seed}`.

###### `src/sda/web/schemas/anonymize.py`
- Что это: схемы для анонимизации.
- Для чего нужен: валидировать правила и preview-ответ.
- Что добавить дальше:
  - модель структуры `columns -> method/salt`,
  - модель preview-ответа.

### Папка `tests/`

`tests` = автоматические тесты.

#### `tests/test_generate.py`
- Что это: тесты генерации.
- Для чего нужен: проверка, что генерация работает корректно.
- Что добавить дальше:
  - тест количества строк/колонок,
  - тест шаблонов `users` и `orders`.

#### `tests/test_anonymize.py`
- Что это: тесты анонимизации.
- Для чего нужен: проверка masking/redaction/pseudonymization.
- Что добавить дальше:
  - тест по каждой стратегии,
  - тест стабильности псевдонимизации,
  - тесты ошибок (неизвестная колонка и т.д.).

#### `tests/test_suggest.py`
- Что это: тесты подсказок PII.
- Для чего нужен: проверка, что рекомендации адекватные.
- Что добавить дальше:
  - тесты на типовые названия колонок,
  - тест confidence-score и граничных случаев.

#### `tests/test_api_generate.py`
- Что это: API-тесты генерации.
- Для чего нужен: проверка web endpoint'ов генерации через `fastapi.testclient`.
- Что добавить дальше:
  - тест `GET /templates`,
  - тест `POST /generate` (status, `text/csv`, скачивание файла).

#### `tests/test_api_anonymize.py`
- Что это: API-тесты анонимизации.
- Для чего нужен: проверка preview/анонимизации и ошибок в web-слое.
- Что добавить дальше:
  - тест `POST /anonymize/preview`,
  - тест `POST /anonymize` (status, `text/csv`, скачивание),
  - тесты читаемых ошибок (пустой CSV, неверные правила, неизвестная колонка).

## Минимальный целевой пользовательский сценарий (MVP)

1. Пользователь отправляет запрос `POST /generate` и получает `users.csv`/`orders.csv`.
2. Пользователь отправляет `POST /anonymize` для своего CSV и передаёт правила.
3. Пользователь получает безопасный CSV для тестов/учебы.
